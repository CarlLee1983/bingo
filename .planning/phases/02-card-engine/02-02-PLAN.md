---
phase: 02-card-engine
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/features/session/session-provider.tsx, src/components/session/BingoCard.tsx, src/components/session/HostPanel.tsx, src/components/session/PlayerRoster.tsx, src/components/session/SessionScreen.tsx, src/styles.css]
autonomous: true
requirements: [CARD-01, CARD-02]
---

<objective>
Render the 5x5 Bingo cards in the UI and provide a "Start Game" control for the host.
Purpose: Allow players to see their generated cards and enable the transition from lobby to active game.
Output: BingoCard component and updated session shell.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-card-engine/02-01-SUMMARY.md
@src/features/session/session-types.ts
@src/components/session/HostPanel.tsx
@src/components/session/PlayerRoster.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BingoCard component</name>
  <files>src/components/session/BingoCard.tsx, src/styles.css</files>
  <action>
    - Create a 5x5 grid component that takes a \`BingoCardGrid\` as a prop.
    - Render a header row with B, I, N, G, O.
    - Render 5 rows of cells.
    - Style the grid with CSS (e.g., CSS Grid or Flexbox) in \`src/styles.css\`.
    - Ensure the 'FREE' square is visually distinct.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>BingoCard component created and styled</done>
</task>

<task type="auto">
  <name>Task 2: Wire Start Game functionality</name>
  <files>src/features/session/session-provider.tsx, src/components/session/HostPanel.tsx</files>
  <action>
    - Update \`SessionProvider\` to expose \`startSession\` which dispatches \`session/start\`.
    - Add a "Start Game" button to \`HostPanel.tsx\` that is only visible/enabled when the session is in 'lobby' status and has at least one player.
    - The button should trigger \`startSession()\`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Host can start the game from the UI</done>
</task>

<task type="auto">
  <name>Task 3: Display player cards in the roster</name>
  <files>src/components/session/PlayerRoster.tsx, src/components/session/SessionScreen.tsx</files>
  <action>
    - Update \`PlayerRoster.tsx\` to render \`BingoCard\` for each player if the session status is 'active' and a card exists for that player.
    - Ensure the layout handles multiple cards gracefully (e.g., a vertical list or a grid of players).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Player cards are visible in the UI after game start</done>
</task>

</tasks>

<verification>
- Host can start the game with a button.
- All players get a visible 5x5 Bingo card in the UI.
- The UI reflects the 'active' game state.
</verification>

<success_criteria>
- The game can transition from Lobby to Active status.
- Bingo cards are correctly rendered for all players.
</success_criteria>

<output>
After completion, create .planning/phases/02-card-engine/02-02-SUMMARY.md
</output>
