---
phase: 02-card-engine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [src/features/session/session-types.ts, src/features/session/session-reducer.ts, src/features/session/session-storage.ts, src/features/session/card-generator.ts, src/features/session/card-generator.test.ts]
autonomous: true
requirements: [CARD-01, CARD-02, CARD-03]
---

<objective>
Implement the core American Bingo card generation logic and integrate it into the session state using TDD.
Purpose: Ensure fair and correct card generation before building the UI.
Output: Tested generator utility and updated session reducer.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/features/session/session-types.ts
@src/features/session/session-reducer.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Card types and update session state</name>
  <files>src/features/session/session-types.ts, src/features/session/session-storage.ts</files>
  <behavior>
    - Define BingoCardGrid as a 5x5 array containing numbers 1-75 or the string 'FREE'.
    - Update SessionState to include a cards Record mapping playerId to BingoCardGrid.
    - Add session/start to SessionAction.
    - Update session-storage.ts to validate the presence of 'cards' in the snapshot.
  </behavior>
  <action>
    - In src/features/session/session-types.ts:
      - Add "export type BingoCardGrid = (number | 'FREE')[][];"
      - Add "cards: Record<string, BingoCardGrid>;" to SessionState.
      - Add "{ type: 'session/start' }" to SessionAction.
    - In src/features/session/session-storage.ts:
      - Update "hasValidSnapshotShape" to check that "snapshot.cards" is an object.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Types defined and project builds successfully with persistence validation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Bingo card generator with TDD</name>
  <files>src/features/session/card-generator.ts, src/features/session/card-generator.test.ts</files>
  <behavior>
    - generateCardGrid() returns a 5x5 grid.
    - Column B (index 0) contains 5 unique numbers from 1-15.
    - Column I (index 1) contains 5 unique numbers from 16-30.
    - Column N (index 2) contains 4 unique numbers from 31-45 and 'FREE' at [2][2].
    - Column G (index 3) contains 5 unique numbers from 46-60.
    - Column O (index 4) contains 5 unique numbers from 61-75.
    - All numbers in a column are unique (no duplicates).
  </behavior>
  <action>
    Create src/features/session/card-generator.ts implementing the logic.
    Create src/features/session/card-generator.test.ts with comprehensive test cases.
  </action>
  <verify>
    <automated>npx vitest src/features/session/card-generator.test.ts --run</automated>
  </verify>
  <done>Card generation logic is fully tested and verified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update reducer to handle session/start</name>
  <files>src/features/session/session-reducer.ts, src/features/session/session-reducer.test.ts</files>
  <behavior>
    - session/start action transitions status from 'lobby' to 'active'.
    - session/start generates a unique card for every player in the session.
    - session/create and session/reset initialize the cards record as empty.
  </behavior>
  <action>
    - Import generateCardGrid in session-reducer.ts.
    - Update sessionReducer to handle session/start.
    - Update createInitialSession and createSession to initialize cards: {}.
    - Add tests to src/features/session/session-reducer.test.ts for session/start.
  </action>
  <verify>
    <automated>npx vitest src/features/session/session-reducer.test.ts --run</automated>
  </verify>
  <done>Reducer correctly handles game start and card distribution</done>
</task>

</tasks>

<verification>
- Bingo cards follow American B-I-N-G-O range rules.
- Free center square is correctly placed.
- Every player gets a card upon game start.
- No duplicate numbers within a card.
</verification>

<success_criteria>
- npm test passes for all session features.
- Card generation logic covers all American Bingo requirements.
</success_criteria>

<output>
After completion, create .planning/phases/02-card-engine/02-01-SUMMARY.md
</output>
