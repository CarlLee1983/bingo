# Plan 05-01: Static Assets and Production Configuration

## Goal
Configure the application for production deployment, ensuring correct asset paths and SPA refresh handling.

## Tasks
1.  **Vite Configuration**: Update `vite.config.ts` to set `base: '/bingo/'`.
2.  **Environment Setup**: Create `src/vite-env.d.ts` for type-safe environment variables and a `.env` template.
3.  **SPA Support (404)**: 
    - Create `public/404.html` with a redirection script.
    - Update `index.html` with a redirection-handler script in the `<head>`.
4.  **Local Build Verification**: Run `npm run build` and verify `dist/index.html` paths.

## Verification
- [ ] `npm run build` completes without errors.
- [ ] `dist/index.html` contains paths starting with `/bingo/` (e.g., `<script src="/bingo/assets/index-xxx.js">`).
- [ ] `dist/404.html` exists.
- [ ] TypeScript compiles correctly with new `vite-env.d.ts`.
