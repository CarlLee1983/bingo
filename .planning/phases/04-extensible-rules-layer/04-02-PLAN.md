---
phase: 04-extensible-rules-layer
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [src/features/session/session-types.ts, src/features/session/session-reducer.ts, src/components/session/WinnerAnnouncement.tsx]
autonomous: true
requirements: [RULE-01, RULE-02]

must_haves:
  truths:
    - "Active pattern list is stored in the session state"
    - "Winning pattern labels are displayed correctly in the UI"
  artifacts:
    - path: "src/features/session/session-reducer.ts"
      provides: "Active pattern awareness"
  key_links:
    - from: "src/components/session/WinnerAnnouncement.tsx"
      to: "src/features/session/win-patterns.ts"
      via: "look up pattern labels by ID"
---

<objective>
Integrate the refactored win detection engine into the session state and update the UI to display user-friendly pattern labels.

Purpose: Complete the transition to data-driven rules by surfacing them through state and UI.
Output: Active patterns in state and labeled wins in the UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-extensible-rules-layer/04-01-SUMMARY.md
@src/features/session/session-types.ts
@src/features/session/session-reducer.ts
@src/components/session/WinnerAnnouncement.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SessionState and Reducer with activePatterns</name>
  <files>src/features/session/session-types.ts, src/features/session/session-reducer.ts</files>
  <action>
    - Add `activePatternIds: string[]` to `SessionState`.
    - Update `createInitialSession` and `createSession` to initialize `activePatternIds` with `STANDARD_PATTERNS`.
    - Update `drawNumber` in `session-reducer.ts` to use `state.activePatternIds` to look up patterns in `WIN_PATTERNS` and pass them to `checkWin`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Session state tracks active patterns and the reducer uses them for win detection.</done>
</task>

<task type="auto">
  <name>Task 2: Update UI to show Pattern Labels</name>
  <files>src/components/session/WinnerAnnouncement.tsx</files>
  <action>
    - Update `WinnerAnnouncement` to look up the winning pattern label from `WIN_PATTERNS` using the `winningPattern` ID from state.
    - If a label exists, display it (e.g., "First Row" instead of "row-1").
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Winner announcement displays the descriptive label of the winning pattern.</done>
</task>

</tasks>

<verification>
- Winners are still detected correctly with standard patterns.
- (Manual check) Winner announcement shows labels like "First Row" or "First Column".
</verification>

<success_criteria>
Winning patterns are fully managed through the registry and state, and presented clearly in the UI.
</success_criteria>

<output>
After completion, create `.planning/phases/04-extensible-rules-layer/04-02-SUMMARY.md`
</output>
