---
phase: 04-extensible-rules-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/features/session/session-types.ts, src/features/session/win-patterns.ts, src/features/session/win-detection.ts, src/features/session/win-detection.test.ts, src/features/session/session-reducer.ts]
autonomous: true
requirements: [RULE-01, RULE-02]

must_haves:
  truths:
    - "Win detection uses pattern definitions from a registry"
    - "Row, column, and diagonal wins are still correctly identified"
  artifacts:
    - path: "src/features/session/win-patterns.ts"
      provides: "Standard and custom win pattern definitions"
    - path: "src/features/session/win-detection.ts"
      provides: "Generalized pattern checking logic"
  key_links:
    - from: "src/features/session/win-detection.ts"
      to: "src/features/session/win-patterns.ts"
      via: "imports pattern definitions"
---

<objective>
Refactor the hardcoded win detection logic into a data-driven system using `WinPattern` types and a pattern registry.

Purpose: Decouple win logic from the core gameplay loop to allow for future variants.
Output: A registry of patterns and a generalized `checkWin` function.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-extensible-rules-layer/04-CONTEXT.md
@src/features/session/session-types.ts
@src/features/session/win-detection.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define WinPattern and create Registry</name>
  <files>src/features/session/session-types.ts, src/features/session/win-patterns.ts</files>
  <behavior>
    - Define `WinPattern` interface with `id: string`, `label: string`, and `squares: [number, number][]`.
    - Create `WIN_PATTERNS` registry in `win-patterns.ts` containing:
      - 5 Rows (e.g., 'row-1' to 'row-5')
      - 5 Columns (e.g., 'column-1' to 'column-5')
      - 2 Diagonals ('diagonal-1', 'diagonal-2')
      - 'four-corners' (corners of the 5x5 grid)
      - 'blackout' (all non-FREE squares)
  </behavior>
  <action>
    - Add `WinPattern` type to `session-types.ts`.
    - Implement the registry in `win-patterns.ts`.
    - Export a constant `STANDARD_PATTERNS` containing IDs for row, col, diag.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>WinPattern type exists and registry contains all standard and custom patterns specified in D-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor checkWin to use patterns</name>
  <files>src/features/session/win-detection.ts, src/features/session/win-detection.test.ts, src/features/session/session-reducer.ts</files>
  <behavior>
    - `checkWin` should accept `grid: BingoCardGrid`, `calledNumbers: number[]`, and `patterns: WinPattern[]`.
    - It should iterate over `patterns` and return the first matching pattern's `id` (or the whole pattern object if preferred, but `id` is current state).
    - Update existing tests in `win-detection.test.ts` to pass the necessary patterns.
  </behavior>
  <action>
    - Modify `checkWin` signature and implementation in `win-detection.ts`.
    - Update `session-reducer.ts` to import `WIN_PATTERNS` and `STANDARD_PATTERNS`, and pass them to `checkWin` (temporarily using hardcoded standard list to avoid breaking state until Plan 02).
    - Update all tests in `win-detection.test.ts` to use the new pattern-based logic.
  </action>
  <verify>
    <automated>npm test src/features/session/win-detection.test.ts</automated>
  </verify>
  <done>Win detection logic is fully data-driven and passes all tests.</done>
</task>

</tasks>

<verification>
- `npm test` passes for win-detection.
- `npm run build` succeeds.
</verification>

<success_criteria>
Win detection is refactored into a registry-based system while maintaining original functionality.
</success_criteria>

<output>
After completion, create `.planning/phases/04-extensible-rules-layer/04-01-SUMMARY.md`
</output>
