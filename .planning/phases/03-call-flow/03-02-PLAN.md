---
phase: 03-call-flow
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [src/features/session/session-provider.tsx, src/components/session/HostPanel.tsx, src/components/session/BingoCard.tsx, src/components/session/PlayerRoster.tsx, src/components/session/CallHistory.tsx, src/components/session/WinnerAnnouncement.tsx, src/styles.css]
autonomous: true
requirements: [CALL-02, WIN-03]

must_haves:
  truths:
    - "Host can trigger number drawing from the UI"
    - "Full call history is visible in the UI"
    - "Called numbers are highlighted on all player cards"
    - "Winners are clearly announced when a bingo is detected"
  artifacts:
    - path: "src/components/session/CallHistory.tsx"
      provides: "Visual list of all called numbers"
    - path: "src/components/session/WinnerAnnouncement.tsx"
      provides: "UI element for celebrating the winner"
  key_links:
    - from: "src/components/session/BingoCard.tsx"
      to: "session.calledNumbers"
      via: "prop for marking numbers"
---

<objective>
Integrate the number drawing logic into the UI, display call history, highlight marked squares, and announce winners.

Purpose: Provide a complete and intuitive user experience for the Bingo gameplay loop.
Output: Enhanced UI components that reflect the current game state and facilitate number calling.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/features/session/session-provider.tsx
@src/components/session/HostPanel.tsx
@src/components/session/BingoCard.tsx
@src/components/session/PlayerRoster.tsx
@src/styles.css

<interfaces>
From Plan 03-01:
- `drawNumber` action added to reducer.
- `calledNumbers`, `winners`, `winningPattern` added to `SessionState`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Session Provider and CSS</name>
  <files>src/features/session/session-provider.tsx, src/styles.css</files>
  <action>
    - Expose `drawNumber` function in `useSession` context.
    - Add CSS classes for:
      - `.bingo-card__cell--marked`: background color for called numbers.
      - `.call-history`: grid or horizontal list for history.
      - `.winner-announcement`: prominent overlay or section.
  </action>
  <verify>
    <automated>grep -q "drawNumber" src/features/session/session-provider.tsx</automated>
  </verify>
  <done>Session provider exposes the draw function and CSS is ready for new UI elements.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Calling UI (HostPanel & CallHistory)</name>
  <files>src/components/session/HostPanel.tsx, src/components/session/CallHistory.tsx, src/components/session/SessionScreen.tsx</files>
  <action>
    - In `HostPanel.tsx`, when `status === 'active'`:
      - Show "Draw Next Number" button.
      - Disable button if `winners.length > 0` or all numbers (75) are called.
      - Show the most recent number from `calledNumbers` prominently.
    - Create `CallHistory.tsx` to render `session.calledNumbers` in a scrollable list (CALL-02).
    - Add `CallHistory` to `SessionScreen.tsx`.
  </action>
  <verify>
    <automated>grep -q "CallHistory" src/components/session/SessionScreen.tsx</automated>
  </verify>
  <done>Host can call numbers and see the history of previous calls.</done>
</task>

<task type="auto">
  <name>Task 3: Highlight Marked Cells and Announce Winners</name>
  <files>src/components/session/BingoCard.tsx, src/components/session/PlayerRoster.tsx, src/components/session/WinnerAnnouncement.tsx, src/components/session/SessionScreen.tsx</files>
  <action>
    - Update `BingoCard.tsx` to accept `calledNumbers: number[]` as a prop and apply `bingo-card__cell--marked` class if a number is in the array.
    - Create `WinnerAnnouncement.tsx` to display a message like "Bingo! [Player Name] won with a [Pattern]!" when `session.winners.length > 0` (WIN-03).
    - Include `WinnerAnnouncement` in `SessionScreen.tsx` (likely at the top).
    - In `PlayerRoster.tsx`, highlight players who are in `session.winners`.
  </action>
  <verify>
    <automated>grep -q "WinnerAnnouncement" src/components/session/SessionScreen.tsx</automated>
  </verify>
  <done>The UI clearly reflects the game progress, marked cells, and victory states.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Full gameplay loop: Calling numbers, marking cards, history, and winner detection.</what-built>
  <how-to-verify>
    1. Start a session with at least one player.
    2. Click "Start Game".
    3. Click "Draw Next Number" repeatedly.
    4. Confirm that:
       - Numbers appear in history.
       - Numbers are highlighted on the player's card.
       - When a line completes, a winner is announced.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Ensure the full game loop works as expected from start to finish.
</verification>

<success_criteria>
- Host can draw numbers.
- History is visible.
- Cards show marked squares.
- Winners are announced when they complete a pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/03-call-flow/03-02-SUMMARY.md`
</output>
