---
phase: 03-call-flow
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [src/features/session/session-types.ts, src/features/session/session-reducer.ts, src/features/session/win-detection.ts, src/features/session/win-detection.test.ts, src/features/session/session-reducer.test.ts]
autonomous: true
requirements: [CALL-01, CALL-03, WIN-01, WIN-02]

must_haves:
  truths:
    - "Numbers can be drawn randomly from 1-75 without replacement"
    - "Winning patterns (rows, columns, diagonals) are correctly detected"
    - "Winners are automatically identified and stored in state after a draw"
  artifacts:
    - path: "src/features/session/win-detection.ts"
      provides: "Bingo win detection logic"
    - path: "src/features/session/session-reducer.ts"
      provides: "Updated reducer handling number drawing and win state"
  key_links:
    - from: "src/features/session/session-reducer.ts"
      to: "src/features/session/win-detection.ts"
      via: "checkWin call during draw action"
---

<objective>
Implement the core number drawing logic and win detection engine for American Bingo using TDD.

Purpose: Ensure the game logic is robust, follows standard bingo rules, and correctly identifies winners as numbers are drawn.
Output: Updated session state management with calling and winning capabilities.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/features/session/session-types.ts
@src/features/session/session-reducer.ts
@src/features/session/card-generator.ts

<interfaces>
From src/features/session/session-types.ts:
```typescript
export type SessionStatus = 'lobby' | 'active';
export type BingoCardGrid = (number | 'FREE')[][];
export type SessionState = {
  // ...
  status: SessionStatus;
  cards: Record<string, BingoCardGrid>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Session State and Reducer for Number Drawing</name>
  <files>src/features/session/session-types.ts, src/features/session/session-reducer.ts, src/features/session/session-reducer.test.ts</files>
  <behavior>
    - Extend `SessionState` to include `calledNumbers: number[]`, `winners: string[]`, and `winningPattern: string | null`.
    - Update `session/start` to initialize `calledNumbers: []`, `winners: []`, and `winningPattern: null`.
    - Add `session/draw` action that:
      - Validates status is 'active' and no winner yet.
      - Selects a random number between 1 and 75 that is NOT in `calledNumbers` (CALL-01, CALL-03).
      - Appends the number to `calledNumbers`.
    - Test: `session/draw` should not repeat numbers and should fail if status is 'lobby'.
  </behavior>
  <action>
    - Add fields to `SessionState` in `session-types.ts`.
    - Update `initialState` and `session/start` in `session-reducer.ts`.
    - Implement the random selection logic in `session/draw`. Use `Math.random` and filter the 1-75 pool against `calledNumbers`.
  </action>
  <verify>
    <automated>npm test src/features/session/session-reducer.test.ts</automated>
  </verify>
  <done>`session/draw` correctly updates `calledNumbers` and avoids duplicates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Pattern-Based Win Detection Engine</name>
  <files>src/features/session/win-detection.ts, src/features/session/win-detection.test.ts</files>
  <behavior>
    - Function `checkWin(grid: BingoCardGrid, calledNumbers: number[]): string | null` (returns pattern name or null).
    - Detects 5 rows, 5 columns, 2 diagonals (WIN-01).
    - 'FREE' square is always considered marked.
    - Patterns names: "row-N", "column-N", "diagonal-1", "diagonal-2" or similar.
    - Test Case 1: Empty `calledNumbers` -> No win.
    - Test Case 2: Full Row -> Win.
    - Test Case 3: Full Column -> Win.
    - Test Case 4: Full Diagonal -> Win.
  </behavior>
  <action>
    - Create `win-detection.ts` with logic to iterate over rows, columns, and diagonals.
    - Use a helper `isSquareMarked(val, calledNumbers)` which returns true for 'FREE' or if `val` is in `calledNumbers`.
  </action>
  <verify>
    <automated>npm test src/features/session/win-detection.test.ts</automated>
  </verify>
  <done>Win detection logic correctly identifies all 12 standard winning patterns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate Win Detection into Reducer</name>
  <files>src/features/session/session-reducer.ts, src/features/session/session-reducer.test.ts</files>
  <behavior>
    - After a number is drawn in `session/draw`, check every player's card for a win using `checkWin`.
    - If one or more winners are found:
      - Add their player IDs to `winners` array (WIN-02).
      - Store the pattern that triggered the win in `winningPattern`.
    - Test: Drawing a number that completes a player's line should populate `winners` and `winningPattern`.
  </behavior>
  <action>
    - Import `checkWin` into `session-reducer.ts`.
    - In the `session/draw` handler, after updating `calledNumbers`, iterate through `state.cards`.
    - For each card, call `checkWin`.
    - If winners found, update `winners` and `winningPattern`.
  </action>
  <verify>
    <automated>npm test src/features/session/session-reducer.test.ts</automated>
  </verify>
  <done>Game automatically detects and stores winners upon number drawing.</done>
</task>

</tasks>

<verification>
Run all session tests to ensure calling and winning logic are correctly integrated.
</verification>

<success_criteria>
- sessionReducer handles drawing numbers without replacement.
- checkWin detects all 12 standard Bingo lines.
- Reducer identifies winners automatically after each draw.
</success_criteria>

<output>
After completion, create `.planning/phases/03-call-flow/03-01-SUMMARY.md`
</output>
